import { Bar, BarChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';
export default function ActivityChart({ data }: { data: { date: string; rounds: number; accuracy: number | null }[] }) {
  return <div className="activity-chart" role="img" aria-label={`Dokončená kola za posledních 14 dní: ${data.map(d => `${d.date} ${d.rounds}`).join(', ')}`}><ResponsiveContainer width="100%" height={220}><BarChart data={data} margin={{ top: 10, right: 10, left: -20, bottom: 0 }}><CartesianGrid stroke="#ffffff0d" vertical={false} /><XAxis dataKey="date" tick={{ fill: '#a0a0b8', fontSize: 11 }} axisLine={false} tickLine={false} minTickGap={20} /><YAxis allowDecimals={false} tick={{ fill: '#a0a0b8', fontSize: 11 }} axisLine={false} tickLine={false} /><Tooltip contentStyle={{ background: '#1b1b27', border: '1px solid #ffffff22', borderRadius: 8, color: '#ebe7dd' }} cursor={{ fill: '#ffffff05' }} /><Bar dataKey="rounds" name="Dokončená kola" fill="#c4955a" radius={[4, 4, 0, 0]} isAnimationActive={false} /></BarChart></ResponsiveContainer></div>;
}
